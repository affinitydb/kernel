IF(CMAKE_SYSTEM_NAME MATCHES Windows)
        cmake_minimum_required(VERSION 2.8)
ENDIF(CMAKE_SYSTEM_NAME MATCHES Windows)
message ("Processing mvstore kernel ")
project (kernel)
SET(KERNEL_TARGET_NAME "mvstore")
add_subdirectory(src)
